---
interface HreflangEntry {
  lang: string;
  url: string;
}

interface Props {
  title: string;
  description: string;
  canonicalUrl?: string;
  noindex?: boolean;
  hreflang?: HreflangEntry[];
  lang?: string;
}

const {
  title,
  description,
  canonicalUrl = Astro.url.href,
  noindex = false,
  hreflang = [],
  lang = 'en',
  bodyClass = '',
} = Astro.props;

const siteName = 'EWO';

const langLabels: Record<string, string> = {
  en: 'English',
  de: 'Deutsch',
  ro: 'Română',
  uk: 'Українська',
  ru: 'Русский',
  es: 'Español',
};

const langPrefix = lang === 'en' ? '' : `/${lang}`;

const navLabels: Record<string, {models: string, studios: string, blog: string, guides: string}> = {
  en: { models: 'For Models', studios: 'For Studios', blog: 'Blog', guides: 'Guides' },
  de: { models: 'Für Models', studios: 'Für Studios', blog: 'Blog', guides: 'Guides' },
  es: { models: 'Para Modelos', studios: 'Para Estudios', blog: 'Blog', guides: 'Guías' },
  ro: { models: 'Pentru Modele', studios: 'Pentru Studiouri', blog: 'Blog', guides: 'Ghiduri' },
  uk: { models: 'Для Моделей', studios: 'Для Студій', blog: 'Блог', guides: 'Гайди' },
  ru: { models: 'Для Моделей', studios: 'Для Студий', blog: 'Блог', guides: 'Гайды' },
};
const nl = navLabels[lang] ?? navLabels.en;

const ctaLabels: Record<string, string> = {
  en: 'Get Started', de: 'Loslegen', es: 'Comenzar',
  ro: 'Începe', uk: 'Почати', ru: 'Начать',
};

const menuLabels: Record<string, string> = {
  en: 'Menu', de: 'Menü', es: 'Menú', ro: 'Meniu', uk: 'Меню', ru: 'Меню',
};

const nav = [
  { href: `${langPrefix}/for-models`, label: nl.models },
  { href: `${langPrefix}/studio-traffic`, label: nl.studios },
{ href: `${langPrefix}/resources`, label: nl.guides },
];

interface FooterLabels {
  tagline: string;
  colServices: string; colResources: string; colRegions: string;
  services: { href: string; label: string }[];
  resources: { href: string; label: string }[];
  regions: { href: string; label: string }[];
  rights: string;
}

const footerData: Record<string, Omit<FooterLabels, 'services' | 'resources' | 'regions'> & {
  services: string[]; resources: string[]; regions: string[];
}> = {
  en: {
    tagline: 'Legitimate audience growth for webcam models & studios worldwide.',
    colServices: 'Services', colResources: 'Resources', colRegions: 'Regions',
    services: ['For Models', 'Studio Traffic', 'New Model Growth', 'Studio Scaling'],
    resources: ['Model Promotion', 'Why Low Viewers', 'Algorithm Guide', 'Income Guide', 'Resources', 'Blog', 'Contact'],
    regions: ['USA', 'Germany', 'Romania', 'Ukraine', 'Colombia', 'Spain'],
    rights: 'All rights reserved',
  },
  de: {
    tagline: 'Seriöses Publikumswachstum für Webcam-Models & Studios weltweit.',
    colServices: 'Services', colResources: 'Ressourcen', colRegions: 'Regionen',
    services: ['Für Models', 'Studio-Traffic', 'Wachstum für neue Models', 'Studio-Skalierung'],
    resources: ['Model-Promotion', 'Warum wenig Zuschauer', 'Algorithmus-Guide', 'Einkommens-Guide', 'Ressourcen', 'Blog', 'Kontakt'],
    regions: ['USA', 'Deutschland', 'Rumänien', 'Ukraine', 'Kolumbien', 'Spanien'],
    rights: 'Alle Rechte vorbehalten',
  },
  es: {
    tagline: 'Crecimiento de audiencia legítimo para modelos webcam y estudios en todo el mundo.',
    colServices: 'Servicios', colResources: 'Recursos', colRegions: 'Regiones',
    services: ['Para Modelos', 'Tráfico para Estudios', 'Crecimiento de Nuevas Modelos', 'Escalado de Estudios'],
    resources: ['Promoción de Modelos', 'Por Qué Pocos Espectadores', 'Guía del Algoritmo', 'Guía de Ingresos', 'Recursos', 'Blog', 'Contacto'],
    regions: ['EE. UU.', 'Alemania', 'Rumanía', 'Ucrania', 'Colombia', 'España'],
    rights: 'Todos los derechos reservados',
  },
  ro: {
    tagline: 'Creștere legitimă de audiență pentru modele webcam și studiouri la nivel mondial.',
    colServices: 'Servicii', colResources: 'Resurse', colRegions: 'Regiuni',
    services: ['Pentru Modele', 'Trafic pentru Studiouri', 'Creștere Modele Noi', 'Extindere Studiouri'],
    resources: ['Promovare Modele', 'De Ce Puțini Vizitatori', 'Ghid Algoritm', 'Ghid Venituri', 'Resurse', 'Blog', 'Contact'],
    regions: ['SUA', 'Germania', 'România', 'Ucraina', 'Columbia', 'Spania'],
    rights: 'Toate drepturile rezervate',
  },
  uk: {
    tagline: 'Легітимне зростання аудиторії для вебкам-моделей і студій у всьому світі.',
    colServices: 'Послуги', colResources: 'Ресурси', colRegions: 'Регіони',
    services: ['Для Моделей', 'Трафік для Студій', 'Зростання Нових Моделей', 'Масштабування Студій'],
    resources: ['Промоція Моделей', 'Чому Мало Глядачів', 'Гід по Алгоритму', 'Гід по Доходу', 'Ресурси', 'Блог', 'Контакт'],
    regions: ['США', 'Німеччина', 'Румунія', 'Україна', 'Колумбія', 'Іспанія'],
    rights: 'Усі права захищені',
  },
  ru: {
    tagline: 'Легитимный рост аудитории для вебкам-моделей и студий по всему миру.',
    colServices: 'Услуги', colResources: 'Ресурсы', colRegions: 'Регионы',
    services: ['Для Моделей', 'Трафик для Студий', 'Рост Новых Моделей', 'Масштабирование Студий'],
    resources: ['Продвижение Моделей', 'Почему Мало Зрителей', 'Гид по Алгоритму', 'Гид по Доходу', 'Ресурсы', 'Блог', 'Контакт'],
    regions: ['США', 'Германия', 'Румыния', 'Украина', 'Колумбия', 'Испания'],
    rights: 'Все права защищены',
  },
};
const fd = footerData[lang] ?? footerData.en;

const footerServiceHrefs = ['/for-models', '/studio-traffic', '/new-model-growth', '/studio-scaling'];
const footerResourceHrefs = ['/model-promotion', '/why-low-viewers-webcam', '/how-cam-algorithm-ranks-rooms', '/webcam-model-income-guide', '/resources', '/blog', '/contact'];
const footerRegionHrefs = ['/model-promotion-usa', '/model-promotion-germany', '/model-promotion-romania', '/model-promotion-ukraine', '/model-promotion-colombia', '/model-promotion-spain'];

const footerServices = fd.services.map((label, i) => ({ href: `${langPrefix}${footerServiceHrefs[i]}`, label }));
const footerResources = fd.resources.map((label, i) => ({ href: `${langPrefix}${footerResourceHrefs[i]}`, label }));
const footerRegions = fd.regions.map((label, i) => ({ href: footerRegionHrefs[i], label }));
---

<!doctype html>
<html lang={lang}>
  <head>
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-6H89TM92EZ"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-6H89TM92EZ');
    </script>

    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} | {siteName}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalUrl} />
    {noindex && <meta name="robots" content="noindex, nofollow" />}
    <meta property="og:title" content={`${title} | ${siteName}`} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:type" content="website" />
    <meta property="og:image" content="https://ewohub.com/logo-neon.png" />
    <meta property="og:site_name" content={siteName} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={`${title} | ${siteName}`} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content="https://ewohub.com/logo-neon.png" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    {hreflang.map(({ lang: l, url }) => (
      <link rel="alternate" hreflang={l} href={url} />
    ))}
    {hreflang.length > 0 && <link rel="alternate" hreflang="x-default" href={hreflang.find(h => h.lang === 'en')?.url ?? canonicalUrl} />}
    <slot name="head" />

    <!-- Organization schema -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "Organization",
      "name": "EWOhub",
      "url": "https://ewohub.com",
      "logo": "https://ewohub.com/logo-neon.png",
      "sameAs": ["https://t.me/+tBjfmjh9vHo1YzQy"]
    })} />

    <!-- WebSite schema -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "WebSite",
      "name": "EWOhub",
      "url": "https://ewohub.com"
    })} />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans:ital,wght@1,300;1,400;1,600;1,700&family=Nunito:wght@400;500;600;700;800;900&display=swap" rel="stylesheet" />
  </head>
  <body class={bodyClass}>
    <header class="site-header">
      <nav class="nav-inner">
        <a href="/" class="logo" aria-label="EWO home">
          <img src="/logo-neon.png" alt="EWO" class="logo-img">
          <span class="logo-ewo">EWO<span class="logo-hub">hub</span></span>
        </a>
        <ul class="nav-links" role="list">
          {nav.map(({ href, label }) => (
            <li><a href={href} class="nav-link">{label}</a></li>
          ))}
        </ul>
        {hreflang.length > 1 && (
          <div class="lang-dropdown">
            <button class="lang-drop-btn" aria-expanded="false" aria-haspopup="true" id="lang-btn">
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>
              <span>{langLabels[lang] ?? lang}</span>
              <svg class="chevron" width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
            </button>
            <ul class="lang-menu" role="listbox" id="lang-menu">
              {hreflang.map(({ lang: l, url }) => (
                <li>
                  <a href={url} class={`lang-opt${l === lang ? ' lang-opt--active' : ''}`} hreflang={l}>
                    {langLabels[l] ?? l}
                  </a>
                </li>
              ))}
            </ul>
          </div>
        )}
        <a href="https://t.me/+tBjfmjh9vHo1YzQy" target="_blank" rel="noopener" class="btn-nav">{ctaLabels[lang] ?? 'Get Started'}</a>
        <button class="mobile-menu-btn" id="mobile-menu-btn" aria-expanded="false" aria-controls="mobile-menu" aria-label={menuLabels[lang] ?? 'Menu'}>
          <svg class="mobile-menu-icon mobile-menu-icon--open" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
          <svg class="mobile-menu-icon mobile-menu-icon--close" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
        </button>
      </nav>

      <div class="mobile-menu" id="mobile-menu">
        <ul class="mobile-nav-links" role="list">
          {nav.map(({ href, label }) => (
            <li><a href={href} class="mobile-nav-link">{label}</a></li>
          ))}
        </ul>
        {hreflang.length > 1 && (
          <ul class="mobile-lang-list" role="list">
            {hreflang.filter(h => h.lang !== 'x-default').map(({ lang: l, url }) => (
              <li>
                <a href={url} class={`mobile-lang-link${l === lang ? ' mobile-lang-link--active' : ''}`} hreflang={l}>
                  {langLabels[l] ?? l}
                </a>
              </li>
            ))}
          </ul>
        )}
        <a href="https://t.me/+tBjfmjh9vHo1YzQy" target="_blank" rel="noopener" class="btn btn-primary mobile-menu-cta">{ctaLabels[lang] ?? 'Get Started'}</a>
      </div>
    </header>


    <main id="main-content">
      <slot />
    </main>

    <footer class="site-footer">
      <div class="footer-inner">
        <div class="footer-brand">
          <div class="footer-logo">
            <img src="/logo-neon.png" alt="EWO" class="logo-img">
            <span class="logo-ewo">EWO<span class="logo-hub">hub</span></span>
          </div>
          <p class="footer-tagline">{fd.tagline}</p>
        </div>
        <nav class="footer-nav" aria-label="Footer navigation">
          <div class="footer-col">
            <p class="footer-col-title">{fd.colServices}</p>
            <ul role="list">
              {footerServices.map(({ href, label }) => (
                <li><a href={href}>{label}</a></li>
              ))}
            </ul>
          </div>
          <div class="footer-col">
            <p class="footer-col-title">{fd.colResources}</p>
            <ul role="list">
              {footerResources.map(({ href, label }) => (
                <li><a href={href}>{label}</a></li>
              ))}
            </ul>
          </div>
          <div class="footer-col">
            <p class="footer-col-title">{fd.colRegions}</p>
            <ul role="list">
              {footerRegions.map(({ href, label }) => (
                <li><a href={href}>{label}</a></li>
              ))}
            </ul>
          </div>
        </nav>
      </div>
      <div class="footer-bottom">
        <p>&copy; {new Date().getFullYear()} EWOhub · {fd.rights}</p>
      </div>
    </footer>
  </body>
</html>

<style is:global>
  /* ══════════════════════════════════════════
     DESIGN TOKENS — edit here to retheme the whole site
     ══════════════════════════════════════════ */
  :root {
    /* ── Backgrounds ── */
    --bg:        #fafaf8;   /* page background */
    --bg-raised: #f4f4f2;   /* cards, raised surfaces */
    --bg-subtle: #f0f0ee;   /* footer, code blocks */

    /* ── Foreground ── */
    --fg:        #0a0a0a;
    --fg-muted:  rgba(10,10,10,0.68);
    --fg-faint:  rgba(10,10,10,0.48);

    /* ── Borders ── */
    --border:        rgba(0,0,0,0.08);
    --border-subtle: rgba(0,0,0,0.05);
    --border-strong: rgba(0,0,0,0.16);

    /* ── Accent (used for interactive highlights, gradient text) ── */
    --accent:       #0a0a0a;
    --accent-2:     #555555;   /* gradient end / secondary accent */
    --accent-hover: #333333;
    --accent-dim:   rgba(0,0,0,0.06);

    /* ── Buttons ── */
    --btn-bg:  #0a0a0a;
    --btn-fg:  #fafaf8;

    /* ── Cards / glass surfaces ── */
    --card-bg:     rgba(0,0,0,0.025);
    --card-border: rgba(0,0,0,0.08);
    --card-shadow: rgba(0,0,0,0.06);

    /* ── Tags / badges ── */
    --tag-bg:     rgba(0,0,0,0.06);
    --tag-border: rgba(0,0,0,0.12);
    --tag-fg:     #1a1a1a;

    /* ── Decorative orbs ── */
    --orb-1: rgba(0,0,0,0.04);
    --orb-2: rgba(0,0,0,0.03);

    /* ── Shadows ── */
    --shadow-sm: rgba(0,0,0,0.06);
    --shadow-md: rgba(0,0,0,0.1);

    /* ── Dividers ── */
    --divider: rgba(0,0,0,0.1);

    /* ── Typography ── */
    --font-heading: 'Josefin Sans', system-ui, sans-serif;
    --font-body:    'Nunito', system-ui, sans-serif;

    --radius-sm: 6px;
    --radius-md: 12px;
    --radius-lg: 16px;

    --transition: 180ms ease;

    --container: 1200px;
    --container-text: 720px;
  }

  /* ── Reset ── */
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  html { font-size: 16px; scroll-behavior: smooth; color-scheme: light; }
  body {
    font-family: var(--font-body);
    background: var(--bg);
    color: var(--fg);
    line-height: 1.65;
    -webkit-font-smoothing: antialiased;
    min-height: 100dvh;
    display: flex;
    flex-direction: column;
    overflow-x: hidden;
  }
  main { flex: 1; }
  img { max-width: 100%; display: block; }
  a { color: inherit; text-decoration: none; }
  ul { list-style: none; }
  button { cursor: pointer; font-family: inherit; }

  /* ── Typography ── */
  h1, h2, h3, h4 {
    font-family: var(--font-heading);
    font-weight: 700;
    font-style: normal;
    line-height: 1.15;
    letter-spacing: -0.01em;
    color: var(--fg);
  }
  h1 { font-size: clamp(2.5rem, 5vw, 4.5rem); }
  h2 { font-size: clamp(1.75rem, 3vw, 2.75rem); }
  h3 { font-size: clamp(1.1rem, 2vw, 1.4rem); font-weight: 600; }
  p  { color: var(--fg-muted); line-height: 1.75; max-width: 65ch; }

  /* ── Layout helpers ── */
  .container {
    max-width: var(--container);
    margin-inline: auto;
    padding-inline: clamp(1.25rem, 4vw, 2rem);
  }
  .section {
    padding-block: clamp(4rem, 8vw, 7rem);
  }
  .divider {
    border: none;
    border-top: 1px solid var(--border);
  }

  /* ── Buttons ── */
  .btn {
    display: inline-flex;
    align-items: center;
    gap: .5rem;
    font-family: var(--font-heading);
    font-size: .9375rem;
    font-weight: 700;
    letter-spacing: .01em;
    padding: .75rem 1.75rem;
    border-radius: var(--radius-md);
    transition: opacity var(--transition), transform var(--transition), box-shadow var(--transition);
    cursor: pointer;
    border: none;
    white-space: nowrap;
    min-height: 48px;
  }
  .btn-primary {
    background: #0a0a0a;
    color: #fff;
  }
  .btn-primary:hover {
    opacity: 0.9;
    box-shadow: 0 0 24px rgba(255,255,255,.15);
  }
  .btn-ghost {
    background: transparent;
    color: var(--fg-muted);
    border: 1px solid var(--border);
  }
  .btn-ghost:hover {
    border-color: rgba(0,0,0,0.18);
    background: rgba(0,0,0,0.04);
    color: var(--fg);
  }
  .btn-nav {
    display: inline-flex;
    align-items: center;
    font-family: var(--font-heading);
    font-size: .875rem;
    font-weight: 700;
    padding: .5rem 1.25rem;
    border-radius: var(--radius-md);
    background: #0a0a0a;
    color: #fff;
    transition: opacity var(--transition), box-shadow var(--transition);
    min-height: 40px;
  }
  .btn-nav:hover {
    opacity: 0.9;
    box-shadow: 0 0 16px rgba(255,255,255,.1);
  }

  /* ── Header ── */
  .site-header {
    position: sticky;
    top: 0;
    z-index: 100;
    background: rgba(250,250,248,0.95);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(0,0,0,0.06);
  }
  .nav-inner {
    max-width: var(--container);
    margin-inline: auto;
    padding-inline: clamp(1.25rem, 4vw, 2rem);
    height: 64px;
    display: flex;
    align-items: center;
    gap: 2rem;
  }
  .logo {
    display: flex;
    align-items: center;
    gap: .5rem;
    flex-shrink: 0;
    text-decoration: none;
  }
  .logo-img { width: 32px; height: 32px; mix-blend-mode: screen; }
  .logo-ewo {
    font-family: 'Nunito', sans-serif;
    font-weight: 900;
    font-size: 1.25rem;
    letter-spacing: -0.01em;
    background: linear-gradient(135deg, #0a0a0a, #555555);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  .logo-hub {
    font-weight: 500;
    opacity: 0.55;
    letter-spacing: 0;
  }
  .nav-links {
    display: flex;
    align-items: center;
    gap: .25rem;
    margin-left: auto;
  }
  .nav-link {
    font-family: var(--font-heading);
    font-size: .9rem;
    font-weight: 600;
    color: var(--fg-muted);
    padding: .5rem .875rem;
    border-radius: var(--radius-sm);
    transition: color var(--transition), background var(--transition);
  }
  .nav-link:hover {
    color: var(--fg);
    background: rgba(0,0,0,0.06);
  }
  @media (max-width: 640px) {
    .nav-links { display: none; }
    .nav-inner { gap: 1rem; }
  }

  /* ── Mobile menu ── */
  .mobile-menu-btn {
    display: none;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    background: transparent;
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    color: var(--fg-muted);
    flex-shrink: 0;
    transition: color var(--transition), border-color var(--transition), background var(--transition);
  }
  .mobile-menu-btn:hover {
    color: var(--fg);
    border-color: rgba(255,255,255,.35);
    background: rgba(0,0,0,0.06);
  }
  .mobile-menu-icon--close { display: none; }
  .mobile-menu-btn[aria-expanded="true"] .mobile-menu-icon--open { display: none; }
  .mobile-menu-btn[aria-expanded="true"] .mobile-menu-icon--close { display: block; }

  .mobile-menu {
    display: none;
    flex-direction: column;
    gap: 1.25rem;
    max-width: var(--container);
    margin-inline: auto;
    padding: 1.25rem clamp(1.25rem, 4vw, 2rem) 1.75rem;
    border-top: 1px solid rgba(0,0,0,0.08);
  }
  .mobile-menu.is-open { display: flex; }

  .mobile-nav-links { display: flex; flex-direction: column; gap: .25rem; }
  .mobile-nav-link {
    display: block;
    font-family: var(--font-heading);
    font-size: 1.0625rem;
    font-weight: 600;
    color: var(--fg);
    padding: .75rem .5rem;
    border-radius: var(--radius-sm);
    transition: color var(--transition), background var(--transition);
  }
  .mobile-nav-link:hover { color: var(--accent); background: rgba(0,0,0,0.04); }

  .mobile-lang-list {
    display: flex;
    flex-wrap: wrap;
    gap: .5rem;
    padding-top: 1rem;
    border-top: 1px solid rgba(0,0,0,0.08);
  }
  .mobile-lang-link {
    font-family: var(--font-heading);
    font-size: .8125rem;
    font-weight: 600;
    color: var(--fg-muted);
    padding: .4rem .875rem;
    border-radius: 99px;
    border: 1px solid var(--border);
    transition: color var(--transition), border-color var(--transition), background var(--transition);
  }
  .mobile-lang-link:hover { color: var(--fg); border-color: rgba(255,255,255,.3); }
  .mobile-lang-link--active { color: var(--accent); border-color: rgba(255,255,255,.4); background: rgba(0,0,0,0.07); }

  .mobile-menu-cta { width: 100%; justify-content: center; }

  @media (max-width: 640px) {
    .mobile-menu-btn { display: flex; }
  }
  @media (min-width: 641px) {
    .mobile-menu { display: none !important; }
  }

  /* ── Cards ── */
  .card {
    background: rgba(0,0,0,0.02);
    border: 1px solid rgba(0,0,0,0.06);
    border-radius: var(--radius-lg);
    padding: 2rem;
    backdrop-filter: blur(12px);
    transition: border-color var(--transition), background var(--transition), box-shadow var(--transition);
  }
  .card:hover {
    border-color: rgba(0,0,0,0.18);
    background: rgba(0,0,0,0.04);
    box-shadow: 0 4px 32px rgba(0,0,0,.1);
  }

  /* ── Accent text ── */
  .text-accent { color: var(--accent); }
  .text-muted  { color: var(--fg-muted); }

  /* ── Footer ── */
  .site-footer {
    border-top: 1px solid rgba(0,0,0,0.08);
    background: var(--bg-subtle);
  }
  .footer-inner {
    max-width: var(--container);
    margin-inline: auto;
    padding: 4rem clamp(1.25rem, 4vw, 2rem) 3rem;
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 4rem;
  }
  .footer-logo {
    display: flex;
    align-items: center;
    gap: .5rem;
    margin-bottom: .75rem;
  }
  .footer-tagline { color: var(--fg-faint); font-size: .875rem; margin-top: .25rem; max-width: 24ch; font-weight: 600; }
  .footer-nav {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }
  .footer-col-title {
    font-family: var(--font-heading);
    font-size: .75rem;
    font-weight: 700;
    letter-spacing: .1em;
    text-transform: uppercase;
    color: var(--fg-faint);
    margin-bottom: 1rem;
    max-width: none;
  }
  .footer-col ul { display: flex; flex-direction: column; gap: .5rem; }
  .footer-col a {
    font-size: .875rem;
    font-weight: 600;
    color: var(--fg-muted);
    transition: color var(--transition);
  }
  .footer-col a:hover { color: var(--fg); }
  .footer-bottom {
    border-top: 1px solid rgba(0,0,0,0.08);
    padding: 1.25rem clamp(1.25rem, 4vw, 2rem);
    text-align: center;
  }
  .footer-bottom p { font-size: .8125rem; color: var(--fg-faint); max-width: none; font-weight: 600; }

  @media (max-width: 768px) {
    .footer-inner { grid-template-columns: 1fr; gap: 2.5rem; }
    .footer-nav { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 480px) {
    .footer-nav { grid-template-columns: 1fr; }
  }

  /* ── Language Switcher ── */
  .lang-switcher {
    background: rgba(250,250,248,0.95);
    border-bottom: 1px solid rgba(0,0,0,0.04);
  }
  .lang-switcher-inner {
    max-width: var(--container);
    margin-inline: auto;
    padding-inline: clamp(1.25rem, 4vw, 2rem);
    height: 40px;
    display: flex;
    align-items: center;
    gap: 1rem;
  }
  .lang-switcher-label {
    font-family: var(--font-heading);
    font-size: .75rem;
    font-weight: 600;
    letter-spacing: .08em;
    text-transform: uppercase;
    color: var(--fg-faint);
    white-space: nowrap;
  }
  .lang-list {
    display: flex;
    align-items: center;
    gap: .25rem;
    list-style: none;
  }
  .lang-link {
    font-family: var(--font-heading);
    font-size: .8125rem;
    font-weight: 500;
    color: var(--fg-muted);
    padding: .25rem .625rem;
    border-radius: var(--radius-sm);
    border: 1px solid transparent;
    transition: color var(--transition), border-color var(--transition), background var(--transition);
    text-decoration: none;
    white-space: nowrap;
  }
  .lang-link:hover {
    color: var(--fg);
    background: rgba(255,255,255,.05);
  }
  .lang-link--active {
    color: var(--fg);
    border-color: rgba(255,255,255,.35);
    background: rgba(0,0,0,0.07);
  }
  .lang-link--active:hover {
    background: rgba(0,0,0,0.07);
  }

  /* ── Language Dropdown ── */
  .lang-dropdown { position: relative; }
  .lang-drop-btn {
    display: flex; align-items: center; gap: .4rem;
    font-family: var(--font-heading); font-size: .8125rem; font-weight: 600;
    color: var(--fg-muted); background: transparent;
    border: 1px solid var(--border);
    padding: .45rem .875rem; border-radius: var(--radius-sm);
    cursor: pointer; transition: color var(--transition), border-color var(--transition), background var(--transition);
    white-space: nowrap;
  }
  .lang-drop-btn:hover,
  .lang-drop-btn[aria-expanded="true"] {
    color: var(--fg); border-color: rgba(255,255,255,.35);
    background: rgba(0,0,0,0.06);
  }
  .lang-drop-btn[aria-expanded="true"] .chevron { transform: rotate(180deg); }
  .chevron { transition: transform var(--transition); }
  .lang-menu {
    position: absolute; right: 0; top: calc(100% + .5rem);
    background: var(--bg-raised); border: 1px solid var(--border);
    border-radius: var(--radius-md); padding: .4rem;
    min-width: 155px; z-index: 200;
    display: none; flex-direction: column; gap: .15rem;
    box-shadow: 0 8px 32px var(--card-shadow);
  }
  .lang-menu.is-open { display: flex; }
  .lang-opt {
    display: block; padding: .5rem .875rem; border-radius: var(--radius-sm);
    font-family: var(--font-heading); font-size: .875rem; font-weight: 600;
    color: var(--fg-muted); transition: background var(--transition), color var(--transition);
  }
  .lang-opt:hover { background: rgba(0,0,0,0.06); color: var(--fg); }
  .lang-opt--active { color: var(--accent); }
  @media (max-width: 640px) { .lang-drop-btn span { display: none; } }

  /* ── Page / hero images ── */
  .page-hero-image {
    margin: 0 0 2.5rem;
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    background: var(--bg-raised);
    overflow: hidden;
  }
  .page-hero-image img {
    width: 100%;
    height: auto;
    display: block;
  }
  .hero-image {
    margin-top: 2.5rem;
    max-width: 640px;
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    background: var(--bg-raised);
    overflow: hidden;
  }
  .hero-image img {
    width: 100%;
    height: auto;
    display: block;
  }

  /* ── Focus ── */
  :focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 3px;
    border-radius: var(--radius-sm);
  }

  /* ── Reduced motion ── */
  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after { transition-duration: .01ms !important; animation-duration: .01ms !important; }
  }

  /* ══════════════════════════════════════════
     GLOBAL CONTENT COMPONENTS — Deep Wine UI
     Use these classes on any page/layout.
  ══════════════════════════════════════════ */

  /* ── Article hero ── */
  .art-hero {
    position: relative; overflow: hidden;
    padding: 5rem 0 4rem;
    background: linear-gradient(160deg, #f6f6f4 0%, #eeeeec 100%);
    border-bottom: 1px solid rgba(0,0,0,0.06);
  }
  .art-orb { position: absolute; border-radius: 50%; filter: blur(90px); pointer-events: none; }
  .art-orb--1 { width: 500px; height: 500px; background: rgba(0,0,0,0.08); top: -200px; left: -150px; }
  .art-orb--2 { width: 400px; height: 400px; background: rgba(0,0,0,0.07); bottom: -150px; right: -100px; }
  .art-grid {
    position: absolute; inset: 0;
    background-image: linear-gradient(rgba(0,0,0,0.03) 1px, transparent 1px), linear-gradient(90deg, rgba(0,0,0,0.03) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse 80% 100% at 30% 50%, black 30%, transparent 100%);
  }
  .art-hero__inner { position: relative; z-index: 1; max-width: 780px; }
  .art-meta { display: flex; align-items: center; gap: .5rem; flex-wrap: wrap; margin-bottom: 1.5rem; }
  .art-tag {
    display: inline-flex; align-items: center; gap: .4rem;
    font-size: .75rem; font-weight: 600; letter-spacing: .04em;
    padding: .3rem .75rem; border-radius: 99px;
    background: rgba(0,0,0,0.07); border: 1px solid rgba(0,0,0,0.15); color: var(--fg);
  }
  .art-tag--green { background: rgba(52,211,153,0.08); border-color: rgba(52,211,153,0.2); color: #34d399; }
  .art-tag--blue { background: rgba(96,165,250,0.08); border-color: rgba(96,165,250,0.2); color: #60a5fa; }
  .dot-green { width: 6px; height: 6px; background: #34d399; border-radius: 50%; }
  .art-title { font-size: clamp(2.2rem,5vw,3.8rem); font-weight: 700; font-style: normal; line-height: 1.1; letter-spacing: -0.02em; color: var(--fg); margin-bottom: 1.25rem; }
  .art-grad { background: linear-gradient(90deg, #0a0a0a, #555555); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
  .art-lead { font-size: 1.125rem; color: rgba(10,10,10,0.42); line-height: 1.75; max-width: 58ch; margin-bottom: 2.5rem; }
  .art-stats-row { display: flex; align-items: center; gap: 2rem; padding-top: 2rem; border-top: 1px solid rgba(0,0,0,0.07); flex-wrap: wrap; }
  .art-stat-item { display: flex; flex-direction: column; gap: .2rem; }
  .art-stat-num { font-size: 1.75rem; font-weight: 800; font-style: normal; color: var(--fg); line-height: 1; }
  .art-stat-lab { font-size: .78rem; color: rgba(10,10,10,0.38); font-weight: 500; letter-spacing: .03em; }
  .art-stat-div { width: 1px; height: 2.5rem; background: rgba(0,0,0,0.1); }

  /* ── Article layout ── */
  .art-body { padding: 4rem 0 6rem; }
  .art-layout { display: grid; grid-template-columns: 200px 1fr; gap: 4rem; align-items: start; }
  @media (max-width: 900px) { .art-layout { grid-template-columns: 1fr; } .art-toc { display: none; } }

  /* ── TOC sidebar (glass) ── */
  .art-toc {
    position: sticky; top: 88px;
    backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px);
    background: rgba(0,0,0,0.04); border: 1px solid rgba(0,0,0,0.09);
    border-radius: 16px; padding: 1.25rem;
  }
  .art-toc__title { font-size: .7rem; font-weight: 700; letter-spacing: .12em; text-transform: uppercase; color: rgba(10,10,10,0.42); margin-bottom: .875rem; max-width: none; }
  .art-toc__list { list-style: none; display: flex; flex-direction: column; gap: .1rem; counter-reset: toc; margin-bottom: 1.25rem; }
  .art-toc__list li { counter-increment: toc; }
  .art-toc__list a { display: flex; align-items: baseline; gap: .5rem; font-size: .8rem; font-weight: 500; color: rgba(10,10,10,0.55); padding: .35rem .5rem; border-radius: 6px; transition: color .2s, background .2s; text-decoration: none; }
  .art-toc__list a::before { content: counter(toc, decimal-leading-zero); font-size: .65rem; color: var(--fg); font-weight: 700; flex-shrink: 0; }
  .art-toc__list a:hover { color: var(--fg); background: rgba(0,0,0,0.06); }
  .art-toc__cta { display: block; text-align: center; font-size: .78rem; font-weight: 600; color: var(--fg); padding: .5rem; border: 1px solid rgba(0,0,0,0.15); border-radius: 8px; background: rgba(0,0,0,0.06); text-decoration: none; transition: background .2s; }
  .art-toc__cta:hover { background: rgba(255,255,255,0.16); }

  /* ── Intro callout ── */
  .art-intro { font-size: 1.05rem; color: rgba(10,10,10,0.52); line-height: 1.8; padding: 1.25rem 1.5rem; border-left: 3px solid #0a0a0a; background: rgba(0,0,0,0.06); border-radius: 0 12px 12px 0; margin-bottom: 3rem; max-width: none; }
  .art-intro strong { color: var(--fg); font-weight: 600; }

  /* ── Reason / section cards (glass) ── */
  .reason-card {
    backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
    background: rgba(0,0,0,0.02); border: 1px solid rgba(0,0,0,0.08);
    border-radius: 18px; padding: 2rem; margin-bottom: 1.25rem;
    display: grid; grid-template-columns: 52px 1fr; gap: 1.5rem;
    transition: border-color .2s, box-shadow .2s;
    box-shadow: 0 1px 3px rgba(0,0,0,.06), 0 4px 12px rgba(0,0,0,.04);
  }
  .reason-card:hover { border-color: rgba(0,0,0,0.16); box-shadow: 0 2px 8px rgba(0,0,0,.05), 0 8px 24px rgba(0,0,0,.06); }
  .reason-num { font-size: 2rem; font-weight: 800; line-height: 1; padding-top: .35rem; color: rgba(0,0,0,0.15); flex-shrink: 0; letter-spacing: -0.02em; }
  .reason-content h2 { font-size: 1.25rem; font-weight: 700; color: var(--fg); margin-bottom: .875rem; letter-spacing: -0.01em; }
  .reason-content p { font-size: .9375rem; color: rgba(10,10,10,0.52); line-height: 1.8; margin-bottom: .875rem; max-width: none; }
  .reason-content em { color: var(--fg); font-style: normal; font-weight: 500; }
  .reason-content strong { color: var(--fg); font-weight: 600; }

  /* ── Glass card (generic) ── */
  .glass-card {
    backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
    background: rgba(0,0,0,0.02); border: 1px solid rgba(0,0,0,0.08);
    border-radius: 16px; padding: 1.75rem;
    box-shadow: 0 1px 3px rgba(0,0,0,.07);
    transition: border-color .2s, box-shadow .2s;
  }
  .glass-card:hover { border-color: rgba(0,0,0,0.16); box-shadow: 0 4px 20px rgba(0,0,0,.08); }

  /* ── Fix box ── */
  .fix-box { background: rgba(52,211,153,0.07); border: 1px solid rgba(52,211,153,0.18); border-radius: 12px; padding: 1rem 1.25rem; margin-top: 1.25rem; }
  .fix-box__header { display: flex; align-items: center; gap: .4rem; font-size: .75rem; font-weight: 700; letter-spacing: .08em; text-transform: uppercase; color: #34d399; margin-bottom: .625rem; }
  .fix-box__header svg { color: #34d399; }
  .fix-box p { font-size: .9rem; color: rgba(10,10,10,0.52); line-height: 1.75; margin: 0; max-width: none; }
  .fix-box a { color: var(--fg); text-decoration: underline; text-underline-offset: 3px; }

  /* ── Pull quote ── */
  .pull-quote { display: flex; gap: .875rem; align-items: flex-start; background: rgba(0,0,0,0.06); border: 1px solid rgba(0,0,0,0.1); border-radius: 12px; padding: 1.125rem 1.25rem; margin: 1.25rem 0; font-size: .975rem; color: rgba(10,10,10,0.55); line-height: 1.7; }
  .pull-quote__icon { color: var(--fg); flex-shrink: 0; margin-top: .1rem; }
  .pull-quote strong { color: var(--fg); font-weight: 600; }

  /* ── Highlight box ── */
  .highlight-box { display: flex; gap: .875rem; align-items: flex-start; background: rgba(0,0,0,0.04); border: 1px solid rgba(0,0,0,0.09); border-radius: 12px; padding: 1rem 1.25rem; margin: 1.25rem 0; }
  .highlight-box svg { flex-shrink: 0; color: var(--fg); margin-top: .1rem; }
  .highlight-box p { font-size: .9rem; line-height: 1.75; margin: 0; color: rgba(10,10,10,0.52); max-width: none; }
  .highlight-box p strong { color: var(--fg); font-weight: 600; }
  .highlight-box--warning { background: rgba(217,119,6,0.07); border-color: rgba(217,119,6,0.18); }
  .highlight-box--warning svg { color: #f59e0b; }
  .highlight-box--green { background: rgba(52,211,153,0.06); border-color: rgba(52,211,153,0.18); }
  .highlight-box--green svg { color: #34d399; }

  /* ── Checklist ── */
  .checklist { background: rgba(0,0,0,0.02); border: 1px solid rgba(0,0,0,0.07); border-radius: 12px; padding: 1.125rem 1.25rem; margin: 1.25rem 0; }
  .checklist__title { font-size: .75rem; font-weight: 700; letter-spacing: .06em; text-transform: uppercase; color: rgba(10,10,10,0.42); margin-bottom: .75rem; max-width: none; }
  .checklist ul { list-style: none; display: flex; flex-direction: column; gap: .5rem; margin: 0; }
  .checklist li { display: flex; align-items: center; gap: .625rem; font-size: .9rem; color: rgba(10,10,10,0.52); }
  .checklist li svg { color: #f87171; flex-shrink: 0; }
  .checklist li.ok svg { color: #34d399; }

  /* ── Two-col comparison ── */
  .two-col-box { display: grid; grid-template-columns: 1fr 1fr; gap: .75rem; margin: 1.25rem 0; }
  @media (max-width: 600px) { .two-col-box { grid-template-columns: 1fr; } }
  .two-col-box__item { padding: .875rem 1rem; border-radius: 12px; border: 1px solid; }
  .two-col-box__item--bad { background: rgba(239,68,68,0.06); border-color: rgba(239,68,68,0.18); }
  .two-col-box__item--good { background: rgba(52,211,153,0.06); border-color: rgba(52,211,153,0.18); }
  .two-col-box__label { font-size: .75rem; font-weight: 700; letter-spacing: .04em; margin-bottom: .4rem; max-width: none; }
  .two-col-box__item--bad .two-col-box__label { color: #f87171; }
  .two-col-box__item--good .two-col-box__label { color: #34d399; }
  .two-col-box__item p:not(.two-col-box__label) { font-size: .875rem; color: rgba(10,10,10,0.52); margin: 0; max-width: none; line-height: 1.65; }

  /* ── Summary card (glass) ── */
  .summary-card { display: flex; gap: 1.25rem; align-items: flex-start; backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px); background: rgba(0,0,0,0.06); border: 1px solid rgba(0,0,0,0.12); border-radius: 18px; padding: 1.75rem; margin: 3rem 0; box-shadow: 0 2px 8px rgba(0,0,0,0.04); }
  .summary-card__icon { width: 44px; height: 44px; border-radius: 10px; background: rgba(0,0,0,0.07); border: 1px solid rgba(0,0,0,0.14); display: flex; align-items: center; justify-content: center; flex-shrink: 0; color: var(--fg); }
  .summary-card h2 { font-size: 1.2rem; font-weight: 700; color: var(--fg); margin-bottom: .625rem; letter-spacing: -0.01em; }
  .summary-card p { font-size: .9375rem; color: rgba(10,10,10,0.52); line-height: 1.8; margin: 0; max-width: none; }
  .summary-card strong { color: var(--fg); font-weight: 600; }

  /* ── FAQ accordion (glass) ── */
  .faq-section { margin: 3rem 0; }
  .faq-section > h2 { font-size: 1.4rem; font-weight: 700; color: var(--fg); margin-bottom: 1.25rem; letter-spacing: -0.01em; }
  .faq-item { border: 1px solid rgba(0,0,0,0.06); border-radius: 14px; margin-bottom: .625rem; background: rgba(0,0,0,0.03); overflow: hidden; transition: border-color .2s, background .2s; }
  .faq-item.is-open { border-color: rgba(129,140,248,0.3); background: rgba(99,102,241,0.07); }
  .faq-chevron {
    display: flex; width: 100%; justify-content: space-between; align-items: center;
    padding: 1.1rem 1.25rem; gap: 1rem;
    background: none; border: none;
    font-family: var(--font-body); font-size: .95rem; font-weight: 600;
    color: var(--fg); text-align: left; cursor: pointer;
    transition: color .2s;
  }
  .faq-chevron:hover { color: var(--accent); }
  .faq-chevron svg { flex-shrink: 0; color: rgba(0,0,0,0.2); transition: transform .25s, color .2s; }
  .faq-item.is-open .faq-chevron { color: var(--accent); }
  .faq-item.is-open .faq-chevron svg { transform: rotate(180deg); color: var(--accent); }
  .faq-body { display: none; padding: 0 1.25rem 1.1rem; }
  .faq-item.is-open .faq-body { display: block; }
  .faq-item p { font-size: .9rem; color: var(--fg-muted); line-height: 1.8; margin: 0; max-width: none; }
  .faq-item p strong { color: var(--accent); font-weight: 600; }

  /* ── Content CTA block (glass) ── */
  .art-cta { position: relative; overflow: hidden; border-radius: 20px; border: 1px solid rgba(0,0,0,0.12); margin: 3rem 0; box-shadow: 0 4px 24px rgba(0,0,0,0.08); }
  .art-cta__orb { position: absolute; width: 400px; height: 400px; background: rgba(0,0,0,0.1); filter: blur(80px); top: -150px; right: -100px; pointer-events: none; }
  .art-cta__inner { position: relative; z-index: 1; padding: 2.5rem; backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); background: rgba(250,250,248,0.95); }
  .art-cta__badge { display: inline-block; font-size: .7rem; font-weight: 700; letter-spacing: .12em; text-transform: uppercase; color: var(--fg); background: rgba(0,0,0,0.07); border: 1px solid rgba(0,0,0,0.15); padding: .3rem .75rem; border-radius: 99px; margin-bottom: 1rem; }
  .art-cta h2 { font-size: 1.6rem; font-weight: 700; color: var(--fg); margin-bottom: .875rem; letter-spacing: -0.02em; }
  .art-cta p { font-size: .9375rem; color: rgba(10,10,10,0.52); line-height: 1.75; max-width: 52ch; margin-bottom: 1.5rem; }
  .art-cta__actions { display: flex; gap: .875rem; flex-wrap: wrap; }
  .art-cta__btn { display: inline-flex; align-items: center; gap: .5rem; padding: .75rem 1.5rem; border-radius: 10px; font-size: .9rem; font-weight: 600; text-decoration: none; transition: opacity .2s, transform .2s; }
  .art-cta__btn:active { transform: scale(.98); }
  .art-cta__btn--primary { background: #0a0a0a; color: #fafaf8; }
  .art-cta__btn--primary:hover { opacity: .9; }
  .art-cta__btn--ghost { background: rgba(0,0,0,0.04); border: 1px solid rgba(0,0,0,0.14); color: rgba(10,10,10,0.55); }
  .art-cta__btn--ghost:hover { border-color: rgba(0,0,0,0.2); color: var(--fg); }

  /* ── Blog category filter bar ── */
  .blog-filter-bar { display: flex; flex-wrap: wrap; gap: .5rem; margin-bottom: 2rem; }
  .blog-filter-btn {
    font-family: var(--font-heading); font-size: .8125rem; font-weight: 600;
    padding: .5rem 1.1rem; border-radius: 99px;
    background: rgba(0,0,0,0.04); border: 1px solid var(--border);
    color: var(--fg-muted); cursor: pointer; white-space: nowrap;
    transition: background var(--transition), border-color var(--transition), color var(--transition);
  }
  .blog-filter-btn:hover { color: var(--fg); border-color: rgba(0,0,0,0.18); background: rgba(0,0,0,0.06); }
  .blog-filter-btn.is-active { background: #0a0a0a; color: #fff; border-color: #0a0a0a; }
  .is-hidden { display: none !important; }

  /* ── Related cards grid ── */
  .related-section { margin-top: 3rem; }
  .related-section > h2 { font-size: 1.25rem; font-weight: 700; color: var(--fg); margin-bottom: 1.25rem; letter-spacing: -0.01em; }
  .analytics-hub-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 1.25rem; }
  .analytics-hub-card { display: flex; gap: 1rem; align-items: flex-start; background: rgba(0,0,0,0.02); border: 1px solid rgba(0,0,0,0.1); border-radius: 16px; padding: 1.25rem; text-decoration: none; transition: border-color 0.2s, background 0.2s; }
  .analytics-hub-card:hover { border-color: rgba(0,0,0,0.2); background: rgba(0,0,0,0.04); }
  .analytics-hub-card__icon { flex-shrink: 0; width: 52px; height: 52px; border-radius: 12px; background: rgba(0,0,0,0.06); border: 1px solid rgba(0,0,0,0.12); display: flex; align-items: center; justify-content: center; }
  .analytics-hub-card__body { flex: 1; }
  .analytics-hub-card__title { font-size: 1rem; font-weight: 700; color: var(--fg); margin: 0 0 0.4rem; line-height: 1.3; }
  .analytics-hub-card__desc { font-size: 0.85rem; color: rgba(10,10,10,0.32); line-height: 1.55; margin: 0 0 0.75rem; }
  .analytics-hub-card__cta { font-size: 0.8rem; font-weight: 600; color: var(--fg); }
  .related-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap: .75rem; }
  .related-card { display: flex; flex-direction: column; gap: .625rem; padding: 1.1rem; backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px); background: rgba(0,0,0,0.02); border: 1px solid rgba(0,0,0,0.07); border-radius: 14px; text-decoration: none; transition: border-color .2s, box-shadow .2s, transform .2s; }
  .related-card:hover { border-color: rgba(0,0,0,0.16); box-shadow: 0 4px 16px rgba(0,0,0,0.06); transform: translateY(-2px); }
  .related-card__icon { width: 36px; height: 36px; border-radius: 8px; background: rgba(0,0,0,0.07); border: 1px solid rgba(0,0,0,0.12); display: flex; align-items: center; justify-content: center; color: var(--fg); flex-shrink: 0; }
  .related-card p { font-size: .875rem; font-weight: 600; color: var(--fg); line-height: 1.45; margin: 0; max-width: none; flex: 1; }
  .related-card span { font-size: .78rem; font-weight: 600; color: var(--fg); }

  /* ── Card publish date ── */
  .card-date { display: block; font-size: .75rem; font-weight: 600; color: var(--fg-faint); margin-top: .25rem; }

  /* ── Icon badge ── */
  .icon-badge { display: inline-flex; align-items: center; justify-content: center; width: 44px; height: 44px; border-radius: 12px; background: rgba(0,0,0,0.07); border: 1px solid rgba(0,0,0,0.14); color: var(--fg); flex-shrink: 0; }
  .icon-badge--green { background: rgba(52,211,153,0.1); border-color: rgba(52,211,153,0.2); color: #34d399; }
  .icon-badge--violet { background: rgba(0,0,0,0.07); border-color: rgba(0,0,0,0.16); color: var(--fg); }
</style>

<script is:inline>
  (function () {
    var btn = document.getElementById('lang-btn');
    var menu = document.getElementById('lang-menu');
    if (!btn || !menu) return;
    btn.addEventListener('click', function (e) {
      e.stopPropagation();
      var open = menu.classList.toggle('is-open');
      btn.setAttribute('aria-expanded', open ? 'true' : 'false');
    });
    document.addEventListener('click', function () {
      menu.classList.remove('is-open');
      btn.setAttribute('aria-expanded', 'false');
    });
    document.querySelectorAll('.lang-opt').forEach(function (a) {
      a.addEventListener('click', function () {
        try { localStorage.setItem('ewo-lang-set', '1'); } catch (e) {}
      });
    });
  })();

  // Mobile menu
  (function () {
    var btn = document.getElementById('mobile-menu-btn');
    var menu = document.getElementById('mobile-menu');
    if (!btn || !menu) return;
    btn.addEventListener('click', function () {
      var open = menu.classList.toggle('is-open');
      btn.setAttribute('aria-expanded', open ? 'true' : 'false');
    });
    menu.querySelectorAll('a').forEach(function (a) {
      a.addEventListener('click', function () {
        menu.classList.remove('is-open');
        btn.setAttribute('aria-expanded', 'false');
      });
    });
    window.addEventListener('resize', function () {
      if (window.innerWidth > 640 && menu.classList.contains('is-open')) {
        menu.classList.remove('is-open');
        btn.setAttribute('aria-expanded', 'false');
      }
    });
  })();

  // FAQ accordion
  document.querySelectorAll('.faq-chevron').forEach(function (btn) {
    btn.addEventListener('click', function () {
      var item = btn.closest('.faq-item');
      var open = item.classList.toggle('is-open');
      btn.setAttribute('aria-expanded', open ? 'true' : 'false');
    });
  });
</script>

<script>
  // FAQPage schema — auto-generated from .faq-item elements on the page
  (function () {
    var items = [];
    document.querySelectorAll('.faq-item').forEach(function (item) {
      var btn = item.querySelector('.faq-chevron span, .faq-chevron > span, summary');
      var body = item.querySelector('.faq-body p, .faq-body, details > p');
      if (!btn || !body) return;
      var q = btn.textContent.trim();
      var a = body.textContent.trim();
      if (q && a) items.push({ '@type': 'Question', 'name': q, 'acceptedAnswer': { '@type': 'Answer', 'text': a } });
    });
    if (items.length === 0) return;
    var script = document.createElement('script');
    script.type = 'application/ld+json';
    script.text = JSON.stringify({ '@context': 'https://schema.org', '@type': 'FAQPage', 'mainEntity': items });
    document.head.appendChild(script);
  })();
</script>




